# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Database
sqlalchemy[asyncio]>=2.0.36
aiosqlite>=0.20.0

# Config
pydantic-settings>=2.5.0
python-dotenv>=1.0.0

# Google AI — new SDK (google-genai v2.x, not the old google-generativeai)
google-genai>=2.0.0

# LangChain / LangGraph
langchain-core>=1.0.0
langchain-google-genai>=4.0.0
langgraph>=1.0.0

# Embeddings (will pull torch as dep)
sentence-transformers>=3.0.0

# Vector DB
qdrant-client>=1.10.0

# YouTube transcript
youtube-transcript-api>=0.6.0

# Video download + metadata
yt-dlp>=2024.10.0

# Audio transcription (faster-whisper uses ctranslate2, NOT torch)
faster-whisper>=1.0.0

# Instagram profile scraping
instaloader>=4.13.0

# Text chunking
tiktoken>=0.7.0

# HTTP
httpx>=0.27.0
aiofiles>=24.0.0
